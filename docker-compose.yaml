services:
  web:
    build: .
    image: starr_query_app
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      DB_HOST=sample-db-host
      DB_USER=sample_user
      DB_PASS=sample_password
      DB_NAME=sample_database
      DB_PORT=3306



